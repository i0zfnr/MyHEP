PROJECT HANDOFF - StudentEdge / e-biasiswa
Date: 2026-07-17
Path: C:\laragon\www\e-biasiswa

Summary
-------
StudentEdge is a Laravel 13 student affairs application covering scholarship,
discipline, student profiles, fines, vehicle stickers, announcements, reports,
admin management, and guard-house student movement tracking.

Current Git State
-----------------
- Branch: main
- HEAD: Add mobile student QR scan flow
- At the last check, main was ready to push to origin/main.
- Restore checkpoint before the mobile/PWA navigation redesign: 0ecaf9c.
- Do not reset, discard, or overwrite these changes. Inspect git status and diff
  before staging or committing anything.

Current Mobile/PWA Design Direction
-----------------------------------
- Preserve the current StudentEdge color palette and visual identity.
- Do not redesign desktop/tablet views while working on the student PWA experience.
- Student mobile/PWA navigation target:
  Home -> Fines -> Scan QR -> Aid -> More
- Profile should not be a bottom navigation tab. Students should access profile
  from the top-right header user/avatar menu.
- The Scan QR tab should be visually emphasized as the central primary action.
- The More tab can expose secondary student destinations such as Campus Movement,
  Vehicle Sticker, Rules, Announcements, and Settings.
- Student Dashboard mobile view should behave as a lightweight app home screen:
  keep urgent alerts, compact welcome/status information, and key actions; avoid
  repeating the full module directory because the bottom navigation handles access.
- Other student pages should be improved one page at a time with mobile-only CSS:
  offense/fine records first, then movement/QR, scholarship/aid, and profile if
  spacing is still poor.
- Keep routes, controllers, validation, database structure, permissions, and
  business workflows unchanged unless the user explicitly requests backend work.

Recent Completed Work
---------------------
- 2026-07-17: Pushed checkpoint commit 0ecaf9c before further student PWA/mobile
  design experiments.
- 2026-07-17: Added a dedicated student Scan QR route/page for the mobile/PWA
  bottom nav. Guard QR links now point to /student/movements/scan?token=...,
  QR tokens remain usable while the checkpoint is active, and the student still
  receives a short one-time movement pass after each successful scan.
- Added the guard admin role and migration:
  database/migrations/2026_07_11_000011_add_guard_role_to_admins_table.php
- Added admin and student movement management, QR generation, scanning, history,
  reporting, guard access, and related navigation.
- Added a Safari-compatible QR scanner fallback using jsQR and device camera video.
  The active scanner now lives on resources/views/student/movements/scan.blade.php.
- Improved responsive login, dashboard, sidebar, header, footer, and dark theme UI.
- Fixed the application shell so the sidebar and header begin at the viewport top.
- Standardized the Admin Dashboard header spacing with other admin pages.
- Updated English and Malay frontend translations across multiple admin/student pages.
- Fixed discipline announcement sidebar active states so only the current item is active.
- Marked the student AI helper as unavailable and removed its quick-action content.
- Fixed redirect translation handling: TranslateFrontendContent now ignores redirects
  and error responses so Laravel redirect headers remain unchanged.
- Added a regression test for unauthenticated Admin Dashboard redirects to /login.
- All 11 migrations were applied successfully at the last check.
- public/storage is linked.

Current Verification Results
----------------------------
- php artisan test: PASS
  - 3 tests, 5 assertions.
  - Coverage is still minimal and does not validate real business workflows.
- npm run build: PASS with Vite 8.0.9.
- php artisan route:list --except-vendor: PASS, 108 routes.
- php artisan migrate:status: PASS, all 11 migrations marked Ran.
- php artisan view:cache: PASS after the latest layout changes.
- composer audit: FAIL because 16 advisories affect 9 installed packages.

Outstanding Priority Work
-------------------------
1. Production/debug configuration
   - The local .env uses APP_ENV=local and APP_DEBUG=true.
   - This is acceptable only for local development. Do not expose detailed errors on
     a real deployment or long-lived public Cloudflare tunnel.
   - Production must use APP_ENV=production and APP_DEBUG=false.
   - Keep environment-specific values out of committed application code.

2. Dependency security updates
   - composer audit reported 16 known advisories affecting Laravel, Symfony, Guzzle,
     and related packages, including high-severity advisories.
   - Update dependencies in a dedicated change, then run the full test/build/manual
     verification suite before deployment.

3. QR token race condition
   - File: app/Http/Controllers/Student/MovementController.php
   - Token lookup and token rotation are separate operations. Concurrent scans could
     both validate the same token before it is rotated.
   - Make validation and rotation atomic using a database transaction with
     lockForUpdate(), or use a conditional atomic update.

4. Duplicate active movement race condition
   - File: app/Http/Controllers/Student/MovementController.php
   - The active-movement check and checkout insert are not in one transaction.
   - Concurrent submissions could create multiple active checkout records.
   - Add transaction/locking protection and, if practical, a database-level invariant.

5. Password reset throttling
   - File: app/Http/Controllers/Auth/LoginController.php
   - Login attempts are throttled, but six-digit verification attempts are not.
   - Add per-reference/IP throttling, failed-attempt counting, and invalidation after a
     small number of failures. Reset completion should remain single-use and atomic.

6. Stale session authorization
   - Files: app/Http/Middleware/RequireSessionRole.php and EnsureAdminScope.php
   - Authorization currently trusts role/scope data stored in the login session.
   - Revalidate that the account still exists and that its current database role matches
     the required scope, especially for system-admin routes.

7. Real feature test coverage
   - Existing tests only cover the homepage and a guest redirect.
   - Add tests for login throttling, admin scopes, stale/deleted accounts, password reset
     attempts, QR single use, concurrent movement submissions, CRUD authorization,
     uploads, exports, and EN/MS locale switching.

8. routes/web.php maintainability
   - routes/web.php is approximately 1,858 lines and contains substantial CRUD/business
     logic in closures.
   - Move scholarship, discipline, announcements, fines, stickers, and exports into
     controllers, Form Requests, and focused service classes in small verified stages.

Architecture / Important Files
------------------------------
- routes/web.php
  Main route definitions and many inline workflows. Treat as high risk when editing.
- app/Http/Controllers/Auth/LoginController.php
  Login, logout, forgot password, verification code, and password reset flows.
- app/Http/Controllers/Admin/MovementController.php
  Movement administration, QR lifecycle, settings, reports, and exports.
- app/Http/Controllers/Student/MovementController.php
  Student scan-pass validation, checkout/return recording, GPS checks, and history.
- app/Http/Middleware/EnsureAdminScope.php
  Admin scope authorization for system, scholarship, discipline, and movement roles.
- app/Http/Middleware/TranslateFrontendContent.php
  Legacy frontend text replacement for successful English HTML responses only.
- resources/views/layouts/app.blade.php
  Shared responsive application shell, sidebar, header, footer, theme, and navigation.
- lang/en.json and lang/ms.json
  Primary JSON translation dictionaries.
- lang/en/frontend_replace.php
  Compatibility replacement map for remaining hardcoded Malay frontend content.
- app/Support/helpers.php
  Shared audit, CSV, cache, and option helpers.
- e-biasiswa.sql
  Root SQL dump. Migrations may not fully recreate all legacy schema/data.

Recommended Next Execution Order
--------------------------------
1. Preserve the current work:
   git status --short --branch
   git diff --stat

2. If continuing the student PWA/mobile redesign, first verify the current
   uncommitted Blade changes on a real mobile viewport or installed PWA context.
   If the result is poor, restore to checkpoint 0ecaf9c or selectively revert the
   two changed Blade files.

3. Complete and verify the QR/movement transaction fixes.

4. Add password reset throttling and live account/role session validation.

5. Add focused feature tests for the above security behavior.

6. Commit the current application changes before attempting dependency updates.

7. Update Composer dependencies in a separate commit and rerun:
   composer audit
   php artisan test
   php artisan route:list --except-vendor
   npm run build
   php artisan migrate:status

8. Manually verify desktop and mobile workflows:
   - Login/logout and forgot-password flow.
   - Admin/student dashboards and language switching.
   - Guard QR generation, Safari/Chrome scanning, checkout, and return.
   - Student scholarship, offense, fine, and sticker workflows.
   - Admin CRUD, approvals, CSV exports, PDF/print, and uploaded files.

Operational Notes
-----------------
- Cloudflare trycloudflare hostnames are temporary. Host changes can make old browser
  cookies/sessions appear stale; clear site data when testing a new tunnel hostname.
- Use HTTPS for camera access on mobile Safari/Chrome. localhost is also a secure context.
- File uploads use the public disk. Verify php artisan storage:link after deployment.
- Do not commit .env or expose APP_DEBUG=true on a public production environment.
- Session, cache, and queue currently use database-backed drivers in the local setup.

Collaboration Notes
-------------------
- The user generally wants changes implemented and verified, not only suggested.
- Keep progress updates concise and explain visible behavior in plain language.
- Preserve unrelated/user changes in the dirty worktree.
- Before any commit or push, inspect status, diff, branch, remote, and recent history.
